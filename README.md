# eediy_public_content

`eediy_public_content` 是 `eediy_wiki` 的公开内容导出仓。

它只承载准备公开发布的内容，不承担人类长期主写作本体。

## 角色

- `blog/`：公开博客文章
- `rl/`：公开专题内容
- `assets/`：公开可发布的资源文件

## 约束

- 不要把私有原始笔记直接堆进这里
- 需要公开的内容应从 `private_*` / `llm_wiki` 导出到这里
- 历史旧博客迁移内容可作为基线保存在这里
